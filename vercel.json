{
  "buildCommand": "prisma generate --no-engine --schema=./prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
